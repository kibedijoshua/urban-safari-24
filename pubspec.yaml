name: aura_app
description: Aura Uganda - Premium African Fashion Marketplace
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  go_router: ^13.2.4
  cached_network_image: ^3.3.1
  provider: ^6.1.5+1
  cupertino_icons: ^1.0.6
  image_picker: ^1.2.1
  url_launcher: ^6.2.5
  firebase_core: ^3.1.0
  firebase_auth: ^5.1.1
  cloud_firestore: ^5.0.2
  firebase_storage: ^12.0.1
  google_sign_in: ^6.2.1
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
